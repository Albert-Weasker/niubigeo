# 真实 Provider 十案例验收

此目录保存固定输入和脱敏后的结果索引。每个案例从目标域名开始，先让模型独立完成域名认知，再从已归档的模型回答中确定本轮可测的竞争对象和中性关键词。

结果仅代表相应 Provider API、模型、联网方式和运行时间。它们不代表消费端网页、真人地区测试或长期市场表现。

原始 Provider 响应与调用账本保存在对应的 `validation/real-provider-10-*` 隔离目录；本目录只保留可公开阅读的派生结果和案例说明。
