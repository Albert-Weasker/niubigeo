# v0.2.0-rc.1 发布候选

**状态：blocked，未公开发布。** 本地镜像、20 个案例和截图可以查看。没有执行 Git 提交、push、公开 Tag/Release、GHCR 上传、官网部署或 latest 提升。

## 实际结果

- 20 个独立域名均实际执行并取得有效域名回答，不是 20 个全部成功的商业结论。
- 初始域名认知 60 次，测量中的域名认知 72 次，中性关键词测试 69 次；另有 3 次预检。
- 11 个案例取得合格关键词，其余 9 个保留未形成关键词测试的状态，不补词。
- 10 个案例部分完成：R02、R04、R06、R07、R10、R12、R13、R14、R18、R19。共保留 18 次分析失败的原始回答。
- R02、R04 各有 3 次实际测量，包括到期调度器触发的运行。任务已暂停。短时间重复不代表长期趋势。
- 累计 204 次收费调用，USD 1.00476325；未知费用调用 0 次。输入 668018 Token，输出 76511 Token。相对于本轮 USD 2 授权剩余 USD 0.99523675，不自动用于追加调用。

[20 个双语案例](../../examples/README.md)直接以 Markdown 展示逐模型原文、竞争对象、关键词、来源和失败状态，无需启动案例服务。NiubiStar 赞助本项目，其他产品收录不代表合作或背书。

## 本地验收

| 范围 | 实际结果 |
| --- | --- |
| npm test | 212 通过、0 失败、10 跳过；跳过的是缺少旧归档文件的 LIVE-01 至 LIVE-10 回放测试 |
| npm run test:examples | 17 通过、0 失败、0 跳过 |
| 阶段 1 至 5 浏览器回归 | 5 个文件、26 个独立场景通过；Trace 中可追溯 564 条独立 expect 步骤 |
| 实际产品浏览器读取 | 20 例通过，53 张产品截图，全部来自同一 rc4 候选镜像 |
| 补充浏览器验收 | 44 条断言通过，包括原文核对、分子分母、创建、重启、删除、备份读取与回滚副本 |
| 官网页面 | 中英文、390/768/1440 三个宽度的 18 组页面布局检查通过 |
| 指标算术复核 | 693 个归档点的分子、分母、数值及样本引用可重算；不等于判断资格全部正确 |

旧失败周期仍保留。各最终验收周期的 before/after Hash 一致。网页与截图读取没有新增模型推理。

## 镜像与来源

本地多架构候选 index Digest：

`sha256:d71d68ff642465580680b40f95692b65eccb67c01e879524dc3241810bcd4f77`

amd64 与 arm64 均构建并运行验证；amd64 在 Docker Desktop 仿真运行。备份副本可在当前候选中读取，并在上一个本地候选中回滚读取。没有在用户数据上试验，不据此承诺任意稳定版本迁移。

构建来自含用户原有修改的工作树，不冒充冻结的 Git commit。本地关联清单位于 `validation/release-v0.2.0-rc.1/release-manifest.json`，连接 OCI Digest、构建输入、研究计划、账本和截图 Hash。该私有目录不是公开下载入口。

## 尚未通过的门禁

- R06、R07、R10、R11、R19 的原始关键词输出共存在 7 项多个“唯一第一名”冲突，当前统计仍显示为可判定的零值。不能解释为已证明目标不是第一名。[逐项证据与 18 条分析失败](../known-issues.md)。文档披露不代表算法修复。
- 其他统计、引用资格及版本缓存边界列于[已知限制](../limitations.md)。算术一致不能代替语义证据充分。
- 未完成全失败案例导出索引的完整场景验收；现有合成回归不能冒充真实全失败案例。
- 未执行实际 GitHub 页面深浅主题渲染验收；已核对新徽标深浅版本的矢量几何相同。
- 候选没有独立产品/文档发布提交，也没有永久公开证据包、镜像和官网地址。源码归属清单仍需发布前逐项审查，不能将整个脏工作树一并提交。
- 产品工作台仍有中文界面限定。双语案例和官网不等于产品全链路英文验收。

机器门禁以 `validation/release-v0.2.0-rc.1/release-adjudication.json` 为准，逐项保留 passed/blocked，不将页面数量当作发布通过。

## 安装与后续发布

未发布候选使用[本地构建方式](../deployment/docker.md)，不要拉取一个不存在的稳定版本。公开发布时只能按授权转发已验收 OCI，并核对 Digest；不得把重新构建的镜像当成截图所用镜像。preview/rc 和默认手工工作流不更新 latest。

[架构](../ARCHITECTURE.md) · [测量方法](../measurement-methodology.md) · [证据规则](../evidence-model.md) · [升级与回滚](../upgrade.md)
